# Change to your tag central project.
tag_central_project_id="my-tag-central-project"

# Change to the list of analytics projects.
datacatalog_analytics_projects_id=["my-analytics-project-1","my-analytics-project-2"]

# Change to your SA name if you wish.
datacatalog_resources_sa_name="terraform-dc-resources-sa"

# Change if you want to use a different region.
tag_template_region="us"

# Change to the list of data governor members.
datacatalog_data_governor_members=["user:data-governor@my-domain.net"]

# Change to the list of data curator members.
datacatalog_data_curator_members=["user:data-curator@my-domain.net"]

# Change to the list of data analyst members.
datacatalog_data_analyst_members=["user:data-analyst@my-domain.net"]